# Copy to .env and fill in the real host, database, credentials. The .env file is gitignored.
# Used by docker compose (env_file) to supply the API's database connection string.
ConnectionStrings__GauDB=Host=your-db-host;Port=5432;Database=your-db;Username=your-user;Password=CHANGEME;SSL Mode=Require
